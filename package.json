{
  "name": "remote-loader",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": "",
  "scripts": {
    "tests": "npm run build && ls examples/esm/ | xargs -I {} npm run test examples/esm/{} hello",
    "test": "time node -r ./dist/suppress-warnings.js --es-module-specifier-resolution=node --experimental-network-imports --loader=./dist/cache-loader.js",
    "build": "npx esbuild src/cli.ts src/cache-loader.ts src/suppress-warnings.ts --bundle --outdir=dist --format=cjs --platform=node"
  },
  "keywords": [],
  "author": "Gitai<i@gitai.me>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.15.3",
    "esbuild": "^0.17.11"
  },
  "optionalDependencies": {
    "@esbuild-kit/esm-loader": "^2.5.5"
  }
}
